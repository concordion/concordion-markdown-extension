apply plugin: 'java'

wrapper {
    gradleVersion = "2.4"
}

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.concordion:concordion:1.5.0'
    compile 'org.pegdown:pegdown:1.5.0'
}

test {
    systemProperties['concordion.output.dir'] = "$reporting.baseDir/spec"
}
